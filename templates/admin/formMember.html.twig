{% extends 'base.html.twig' %}

{% block body %}
    {% if editMode %}
        <h1>Modification d'un Membre du bureau</h1>
    {% else %}
        </br>
        <h1 style="text-align: center">Ajout d'un Membre du bureau</h1></br>
    {% endif %}

    <div class="container">
        <div class="row">
            <div class="col-12">
                {{ form_start(FormMember) }}
                {{ form_row(FormMember.lastname,{'attr': {'placeholder':"Nom "}}) }}
                {{ form_row(FormMember.firstname,{'attr': {'placeholder':"Prénom "}}) }}
                {{ form_row(FormMember.email) }}
                {{ form_row(FormMember.phone) }}
                {{ form_row(FormMember.role) }}
                {{ form_row(FormMember.address) }}
                {{ form_row(FormMember.grade) }}
                {{ form_row(FormMember.link) }}
                {{ form_row(FormMember.description) }}
                {{ form_row((FormMember.photo)) }}
                </br>
                <button type="submit" class="btn btn-success">
                    {% if editMode %}
                         Enregistrer les modifications
                         {% else %}
                         Ajouter le membre
                     {% endif %}
                </button>
                {{ form_end(FormMember) }}

            </div>
        </div>
    </div>
    </br>
{% endblock %}

{% block title %}

{% endblock %}

