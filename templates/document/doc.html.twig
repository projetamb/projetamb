{% extends 'base.html.twig' %}

{% block title %}{{ pagetitle }}{% endblock %}

{% block body %}
    <div class="container club_docs">
        <h2 class="text-center club_h2">Documents à télécharger</h2>
        <h4>Vous pouvez télécharger les documents en cliquant sur les liens ci-dessous</h4>

        <div class="row">
            {% for file in files %}
                <div class="col-12 col-md-6 col-lg-4 mb-5">
                    <a href="./docs/{{ file.link }}" target="_blank">{{ file.link }}</a> ({{ file.size }})
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}
